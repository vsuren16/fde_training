# MCP Server Demo

Minimal MCP server with one tool: `hello_world`.

## Setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

## Run

```powershell
python server.py
```

This starts the server over stdio (default for `FastMCP`).
