services:
  backend:
    build: ./backend
    ports:
      - "8050:8050"
    env_file:
      - ./backend/.env.example

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_BASE=http://localhost:8050
    depends_on:
      - backend
